apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: cluster-01
  region: ap-south-1
nodeGroups:
  - name: nodegroup-1
    instanceType: t3.medium
    desiredCapacity: 1
    volumeSize: 20
    ssh:
      allow: false
    